export const constant = {
  PREFIX: {
    GLOBAL_PREFIX: '/api',
    SWAGGER_PREFIX: '/api/doc',
  },

  BASE_URL: 'http://localhost:3000', // This can be stored in .env file.
  CONNECTION_STRING: 'mongodb://localhost:27017/feedbacks', // This can be stored in .env file.
};
